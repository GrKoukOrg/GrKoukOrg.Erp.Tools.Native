<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pageModels="clr-namespace:GrKoukOrg.Erp.Tools.Native.PageModels"
             xmlns:models="clr-namespace:GrKoukOrg.Erp.Tools.Native.Models"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:DataType="pageModels:BusinessBuyDocumentsListPageModel"    
             x:Class="GrKoukOrg.Erp.Tools.Native.Pages.BusinessBuyDocumentsListPage">
    <ContentPage.Behaviors>
        <toolkit:EventToCommandBehavior
            EventName="Appearing"
            Command="{Binding AppearingCommand}" />
    </ContentPage.Behaviors>
    
    <ScrollView>
        
        <VerticalStackLayout 
            BindableLayout.ItemsSource="{Binding Items}" 
            Margin="{StaticResource LayoutPadding}" 
            Spacing="{StaticResource LayoutSpacing}">
            <BindableLayout.ItemTemplate>
                <DataTemplate x:DataType="models:BuyDocumentDto">
                    <Border>
                        <VerticalStackLayout Padding="10">
                            <Label Text="{Binding SupplierName}" FontSize="24" />
                            <Label Text="{Binding TransDate, StringFormat='{0:dd/MM/yyyy}'}" />
                            <Label Text="{Binding NetAmount, StringFormat='{}{0:C}'}" />
                            <Label Text="{Binding VatAmount, StringFormat='{}{0:C}'}" />
                            <Label Text="{Binding TotalAmount, StringFormat='{}{0:C}'}" />
                            
                             
                        </VerticalStackLayout>
                       
                    </Border>
                </DataTemplate>
            </BindableLayout.ItemTemplate>
        </VerticalStackLayout>

       
    </ScrollView>
</ContentPage>